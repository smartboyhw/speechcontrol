cmake_minimum_required(VERSION 2.8)

install(FILES "speechcontrol.png"
        DESTINATION "${CMAKE_INSTALL_PREFIX}/share/icons")

install(FILES "speechcontrol.svg"
        DESTINATION "${CMAKE_INSTALL_PREFIX}/share/icons")